/**
 * Convert HTML files from a directory into a PowerPoint presentation.
 *
 * The implementation will be added once the technical details are finalized.
 *
 * @param {object} options
 * @param {string} options.inputDir Directory containing HTML files.
 * @param {string} options.outputFile Destination PPTX file path.
 * @returns {Promise<void>}
 */
export async function convertHtmlDirectoryToPptx(options) {
  void options;
  throw new Error('convertHtmlDirectoryToPptx is not implemented yet.');
}
